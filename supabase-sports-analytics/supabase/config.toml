project_id = "sptahazcjnorayjkltdx"

# This local configuration defaults to the Baseball analytics project after the
# September 2026 MLB/NFL split. Every operational command must still pass the
# league-specific --project-ref; Football is a separate project documented in
# sports-analytics-project-layout.md.
# No sports warehouse may receive storefront products, customers, orders,
# payments, or commerce credentials.
